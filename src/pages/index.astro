---
import "@fontsource-variable/dm-sans";
import "@fontsource-variable/outfit";
import "../styles/shared.css";
import { ViewTransitions } from "astro:transitions";
import Container from "../components/Container.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import * as about from "../pages/about/about.md";
import criticalCss from "../styles/critical.css?raw";

// TypeScript interface for contact markdown files
interface ContactFile {
  frontmatter: {
    title: string;
    url: string;
    icon: string;
  };
}

const siteUrl = "https://nikhilrathore.com";
const siteName = `${about.frontmatter.name} - ${about.frontmatter.designation}`;
const description = `${about.frontmatter.name} - ${about.frontmatter.designation}. Full-Stack Developer skilled in building scalable web applications using React, Next.js, Node.js, and modern UI frameworks. Experienced in developing RESTful APIs, managing databases with PostgreSQL and MongoDB, and deploying production-ready apps.`;
const imageUrl = `${siteUrl}/snap_laptop.webp`;

const contactLinksGlob = import.meta.glob<ContactFile>("../pages/contact/*.md", { eager: true });
const contactLinks = Object.values(contactLinksGlob);
const sameAsLinks = [
  "https://github.com/Blazeiscoding",
  ...contactLinks.map(item => item.frontmatter.url).filter(Boolean)
];
---

<html lang="en">
  <head>
    <script is:inline>
      (function() {
        const savedTheme = localStorage.getItem("theme");
        const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
        const theme = savedTheme || (prefersDark ? "black" : "lofi");
        document.documentElement.setAttribute("data-theme", theme);
      })();
    </script>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/favicon.webp" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    
    <!-- Primary Meta Tags -->
    <title>{siteName}</title>
    <meta name="title" content={siteName} />
    <meta name="description" content={description} />
    <meta name="author" content={about.frontmatter.name} />
    <meta name="keywords" content="Full Stack Developer, React, Next.js, Node.js, TypeScript, Web Developer, Portfolio" />
    <meta name="robots" content="index, follow" />
    <meta name="language" content="English" />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={siteUrl} />
    <meta property="og:title" content={siteName} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={imageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="en_US" />
    
    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={siteUrl} />
    <meta name="twitter:title" content={siteName} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageUrl} />
    
    <!-- Additional SEO -->
    <link rel="canonical" href={siteUrl} />
    <meta name="theme-color" content="#ffffff" />
    
    <!-- Resource Hints for Performance -->
    <link rel="preconnect" href="https://api.github.com" crossorigin />
    <link rel="dns-prefetch" href="https://api.github.com" />
    <link rel="preconnect" href="https://ghchart.rshah.org" crossorigin />
    <link rel="dns-prefetch" href="https://ghchart.rshah.org" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="dns-prefetch" href="https://fonts.googleapis.com" />
    
    <!-- Preload LCP Hero Image -->
    <link rel="preload" as="image" href="/profile.webp" type="image/webp" fetchpriority="high" />
    
    <!-- Preload Critical Fonts -->
    <link rel="preload" as="font" type="font/woff2" href="/_astro/dm-sans-latin-wght-normal.woff2" crossorigin="anonymous" />
    <link rel="preload" as="font" type="font/woff2" href="/_astro/outfit-latin-wght-normal.woff2" crossorigin="anonymous" />
    
    <!-- Critical CSS - Inlined for LCP optimization -->
    <style set:html={criticalCss} />
    
    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Person",
      "name": about.frontmatter.name,
      "jobTitle": about.frontmatter.designation,
      "url": siteUrl,
      "sameAs": sameAsLinks,
      "address": {
        "@type": "PostalAddress",
        "addressLocality": about.frontmatter.location.split(",")[1]?.trim() || about.frontmatter.location,
        "addressCountry": "IN"
      },
      "image": imageUrl,
      "description": description
    })} />
    <ViewTransitions />
  </head>
  <body class="flex flex-col min-h-screen relative">
    <div id="scroll-progress" class="fixed top-0 left-0 h-1 bg-primary z-[10000] transition-all duration-100 ease-out" style="width: 0%"></div>
    
    <Header />

    <div
      class="divider mx-auto h-fit max-w-3xl w-[95vw] flex flex-nowrap bg-secondary"
      role="separator"
      aria-hidden="true"
    >
    </div>
    <main
      class="mx-auto flex-grow max-w-3xl w-[95vw] py-6 px-4 md:px-0 grid gap-4 fade-in-up"
      role="main"
      aria-label="Portfolio content"
    >
      <div class="container">
        <Container />
      </div>
    </main>
    
    <script>
      import { initThemeToggle } from '../scripts/theme';
      import { initScrollProgress, initScrollAnimations, initSmoothScroll } from '../scripts/scroll';
      import { initAnimatedFavicon, initConsoleEasterEgg } from '../scripts/fun';
      
      // Initialize scroll progress immediately (critical for UX)
      initScrollProgress();
      
      // Initialize critical features after DOM is ready
      document.addEventListener("DOMContentLoaded", () => {
        initThemeToggle();
        initScrollAnimations();
        initSmoothScroll();
        
        // Defer non-critical scripts to idle time
        const deferToIdle = (fn: () => void) => {
          if ('requestIdleCallback' in window) {
            requestIdleCallback(fn, { timeout: 2000 });
          } else {
            setTimeout(fn, 200);
          }
        };
        
        deferToIdle(initAnimatedFavicon);
        deferToIdle(initConsoleEasterEgg);
      });
    </script>
    
    <!-- Vercel Analytics - deferred to idle -->
    <script>
      import { inject } from '@vercel/analytics';
      if ('requestIdleCallback' in window) {
        requestIdleCallback(() => inject(), { timeout: 3000 });
      } else {
        setTimeout(() => inject(), 100);
      }
    </script>
    
    <!-- Vercel Speed Insights - deferred to idle -->
    <script>
      import { injectSpeedInsights } from '@vercel/speed-insights';
      if ('requestIdleCallback' in window) {
        requestIdleCallback(() => injectSpeedInsights(), { timeout: 3000 });
      } else {
        setTimeout(() => injectSpeedInsights(), 100);
      }
    </script>
    <Footer class="flex justify-center items-center py-4" />
  </body>
</html>

<style is:global>
  html {
    scroll-behavior: smooth;
    color-scheme: light dark;
  }
  
  body {
    font-family: "DM Sans Variable", sans-serif;
    min-height: 100vh;
    background-color: oklch(var(--b1));
    overflow-x: hidden;
  }

  /* Animated Gradient Background */
  body::before {
    content: "";
    position: fixed;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: oklch(var(--b1));
    animation: gradientAppear 0.5s ease-out 0.1s forwards, gradientMove 20s ease-in-out 0.6s infinite;
    pointer-events: none;
    z-index: -2;
    opacity: 0;
  }
  
  @keyframes gradientAppear {
    to {
      opacity: 1;
      background: 
        radial-gradient(circle at 20% 20%, oklch(var(--p) / 0.2) 0%, transparent 40%),
        radial-gradient(circle at 80% 20%, oklch(var(--s) / 0.2) 0%, transparent 40%),
        radial-gradient(circle at 80% 80%, oklch(var(--a) / 0.2) 0%, transparent 40%),
        radial-gradient(circle at 20% 80%, oklch(var(--n) / 0.2) 0%, transparent 40%);
    }
  }

  @keyframes gradientMove {
    0%, 100% {
      transform: translate(0, 0) rotate(0deg);
    }
    25% {
      transform: translate(5%, 5%) rotate(2deg);
    }
    50% {
      transform: translate(-5%, 10%) rotate(-2deg);
    }
    75% {
      transform: translate(10%, -5%) rotate(1deg);
    }
  }

  /* Parallax Noise Layer - Deferred for performance */
  body::after {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.4'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: -1;
    opacity: 0;
    mix-blend-mode: overlay;
    transform: translateZ(0);
    animation: noiseAppear 0.3s ease-out 0.8s forwards;
  }
  
  @keyframes noiseAppear {
    to { opacity: 0.1; }
  }
  
  /* Circular reveal theme transition */
  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation: none;
    mix-blend-mode: normal;
  }
  
  ::view-transition-old(root) {
    z-index: 1;
  }
  
  ::view-transition-new(root) {
    z-index: 9999;
    animation: circular-reveal 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  @keyframes circular-reveal {
    from {
      clip-path: circle(0% at var(--theme-x, 50%) var(--theme-y, 50%));
    }
    to {
      clip-path: circle(150% at var(--theme-x, 50%) var(--theme-y, 50%));
    }
  }
  
  /* Reverse animation for going to light mode */
  [data-theme="lofi"]::view-transition-new(root) {
    animation: circular-reveal 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  /* Fallback transitions for Firefox and browsers without View Transition API */
  @supports not (view-transition-name: none) {
    html, body,
    header, main, footer,
    .card, .btn, .badge,
    .prose, .collapse,
    article, section,
    div, p, h1, h2, h3, h4, h5, h6 {
      transition: background-color 0.35s cubic-bezier(0.4, 0, 0.2, 1), 
                  color 0.35s cubic-bezier(0.4, 0, 0.2, 1), 
                  border-color 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    }

    h1, h2, h3, h4, h5, h6 {
      font-family: "Outfit Variable", sans-serif;
    }
    
    /* Exclude interactive elements */
    input, button, a, img, svg, video, iframe,
    .toggle, input[type="checkbox"], input[type="radio"] {
      transition: none !important;
    }
  }
  /* Container responsive layout */
  @media (min-width: 768px) {
    .container {
      display: flex;
      flex-wrap: wrap;
    }
  }

  @media (max-width: 768px) {
    .container {
      flex-direction: column;
    }
  }
</style>
